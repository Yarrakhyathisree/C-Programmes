#include<stdio.h>
int main()
{
	char ch;
	printf("Enter any character:");
	scanf("%d",&ch);
	if(ch=='a'||ch=='e'||ch=='i'||ch=='o'||ch=='u');
	printf("\n%c are vowels",ch);
	else("\n%c is not vowels");
	return 0;
}

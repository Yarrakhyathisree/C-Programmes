#include<stdio.h>
int main()
{
	/*C Programme for area and volume of a cube*/
	int area,volume,a;
	printf("area of a cube:");
	scanf("%d%d",&area,&volume);
	area=6*a*a*a;
	printf("area of a cube=%d",area);
	printf("volume of a cube:");
	volume=a*a*a;
	printf("volume of a cube=%d",volume);
	return 0;
}

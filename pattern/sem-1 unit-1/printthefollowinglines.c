#include<stdio.h>
int main()
{
	/*Write a program that will read a number from the keyboard and print the
following in one line.
Smallest integer   The given Largest   number not
not less than   number   greater than the
number							 number*/
printf("smallest integer	the given largest		number not\n");
printf("not less than		number		greater than the\n");
printf("number						number\n");
return 0;
}
